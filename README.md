# FarmWar

基于godot的小游戏，刚开始做，种地，然后天下无敌？





基于python的配置项管理工具

  启动方式：
  cd pythonTool/configWebEditor
  pip install flask
  python server.py
  # 默认读取 ../../config，可用 --config-dir 指定
  # 浏览器打开 http://localhost:5000

  快捷键：Ctrl+S 保存，右键单元格打开备注面板


